{
  "name": "accountant24",
  "version": "0.1.9",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "bun node_modules/@earendil-works/pi-coding-agent/dist/cli.js --offline --no-extensions --no-skills --no-prompt-templates -e packages/pi-extension/src/entry.ts",
    "build": "bun run scripts/build.ts",
    "build:all": "bun run scripts/build.ts --all",
    "test": "bun test",
    "audit": "bun audit --audit-level=moderate",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check --write .",
    "verify": "bun run audit && biome check --write . && biome check . && tsc --noEmit && bun run test --coverage",
    "eval": "bun run packages/evals/run.ts",
    "pre-commit": "biome check --write . && biome check .",
    "release": "bun run scripts/release.ts",
    "release:dry": "bun run scripts/release.ts --dry-run",
    "prepare": "simple-git-hooks"
  },
  "simple-git-hooks": {
    "commit-msg": "commitlint --edit $1",
    "pre-commit": "bun run pre-commit"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ],
    "rules": {
      "header-max-length": [
        2,
        "always",
        72
      ],
      "body-max-line-length": [
        1,
        "always",
        100
      ],
      "scope-enum": [
        2,
        "always",
        [
          "tools",
          "commands",
          "skills",
          "prompt"
        ]
      ]
    }
  },
  "changelog": {
    "repo": "machulav/accountant24",
    "noAuthors": true,
    "types": {
      "feat": {
        "title": "🚀 Features",
        "semver": "minor"
      },
      "fix": {
        "title": "🐞 Bug Fixes",
        "semver": "patch"
      },
      "perf": {
        "title": "⚡ Performance",
        "semver": "patch"
      },
      "refactor": {
        "title": "♻️ Refactors",
        "semver": "patch"
      },
      "revert": {
        "title": "⏪ Reverts",
        "semver": "patch"
      },
      "build": {
        "title": "📦 Build",
        "semver": "patch"
      },
      "docs": {
        "title": "📖 Documentation"
      },
      "chore": {
        "title": "🏡 Chore"
      },
      "test": {
        "title": "✅ Tests"
      },
      "style": {
        "title": "🎨 Styles"
      },
      "ci": {
        "title": "🤖 CI"
      }
    }
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.8",
    "@commitlint/cli": "20.5.0",
    "@commitlint/config-conventional": "20.5.0",
    "@earendil-works/pi-coding-agent": "0.79.8",
    "@types/bun": "1.3.13",
    "changelogen": "0.6.2",
    "simple-git-hooks": "2.13.1",
    "typescript": "5.9.3"
  },
  "overrides": {
    "@earendil-works/pi-ai": "0.79.8",
    "@earendil-works/pi-agent-core": "0.79.8",
    "@earendil-works/pi-tui": "0.79.8"
  }
}

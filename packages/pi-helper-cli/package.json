{
  "name": "@accountant24/pi-helper-cli",
  "version": "0.1.9",
  "private": true,
  "type": "module",
  "description": "Headless helper for the desktop app — wraps pi APIs (auth, models, sessions) the RPC mode can't expose.",
  "dependencies": {
    "@earendil-works/pi-ai": "0.79.8",
    "@earendil-works/pi-coding-agent": "0.79.8"
  }
}
